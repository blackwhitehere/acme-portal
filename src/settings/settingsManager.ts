import * as vscode from 'vscode';

export class SettingsManager {
    private static readonly CONFIG_SECTION = 'acmeportal';

    // This class is kept for future extension settings
    // Currently all flow discovery is handled by the acme-portal-sdk
}
